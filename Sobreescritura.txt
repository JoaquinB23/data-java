Sobreescritura

🔹 ¿Qué es la sobreescritura?

La sobreescritura es una característica de la herencia en Java que permite que una subclase redefina el comportamiento de un método heredado de su superclase.

👉 En otras palabras:

La subclase toma un método existente y le da su propia implementación.

El nombre, los parámetros y el tipo de retorno deben ser los mismos que en el método de la superclase.

🔹 Reglas de la sobreescritura

Mismo nombre, parámetros y tipo de retorno.

El método de la subclase no puede tener un nivel de acceso más restrictivo.

Ej: si en la superclase es public, en la subclase no puede ser private.

Se puede usar la anotación @Override (recomendado) para indicar que se está sobrescribiendo.

El método de la subclase puede lanzar menos excepciones, pero no más.

🔹 Ejemplo básico
class Animal {
    void hacerSonido() {
        System.out.println("El animal hace un sonido");
    }
}

class Perro extends Animal {
    @Override
    void hacerSonido() {
        System.out.println("El perro ladra: Guau guau!");
    }
}

public class Main {
    public static void main(String[] args) {
        Animal a = new Animal();
        a.hacerSonido(); // El animal hace un sonido

        Animal p = new Perro();
        p.hacerSonido(); // El perro ladra: Guau guau!
    }
}


👉 Fijate que aunque la variable es de tipo Animal, se ejecuta el método de Perro.
Eso se llama polimorfismo dinámico.

🔹 Diferencia entre sobrecarga (overloading) y sobreescritura (overriding)
Característica	Sobrecarga (Overloading)	Sobreescritura (Overriding)
Clase	En la misma clase o subclase	Siempre en subclase
Métodos	Mismo nombre, distintos parámetros	Mismo nombre, mismos parámetros
Tipo de retorno	Puede cambiar	Debe ser igual o compatible
En tiempo	Se resuelve en compilación	Se resuelve en ejecución