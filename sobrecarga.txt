Sobreescritura en Java (Overriding)

La sobreescritura de métodos consiste en redefinir un método heredado de una superclase en una subclase, manteniendo la misma firma (nombre, parámetros y tipo de retorno compatible).

Permite cambiar o extender el comportamiento de un método existente.

🔹 Reglas de la sobreescritura

El método debe tener el mismo nombre.

Debe tener los mismos parámetros (lista idéntica).

El tipo de retorno debe ser el mismo o un subtipo (covariante).

El modificador de acceso no puede ser más restrictivo.

Ejemplo: si es public en la superclase, debe seguir siendo public.

No se pueden sobrescribir métodos static, final o private.

Se puede usar la anotación @Override (recomendado) para validar en compilación.

🔹 Ejemplo
class Animal {
    void hacerSonido() {
        System.out.println("Sonido genérico");
    }
}

class Perro extends Animal {
    @Override
    void hacerSonido() {
        System.out.println("Guau guau!");
    }
}

public class Main {
    public static void main(String[] args) {
        Animal a = new Perro();
        a.hacerSonido();  // "Guau guau!"
    }
}


👉 Aquí, Perro sobrescribe el método hacerSonido de Animal.

🔹 Diferencia con Sobrecarga (Overloading)

Sobrecarga: mismo nombre de método pero parámetros diferentes (dentro de la misma clase).

Sobreescritura: redefinir un método heredado, con la misma firma, en una subclase.

✅ Resumen 	
La sobreescritura (overriding) en Java permite que una subclase cambie el comportamiento de un método definido en la superclase, manteniendo la misma firma. Se usa para implementar polimorfismo dinámico.

