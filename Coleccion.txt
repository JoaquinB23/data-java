🔹 ¿Qué es una colección?

Una colección es un contenedor de objetos que permite almacenarlos, manipularlos y recorrerlos.

Se agrupan en el framework de colecciones de Java (Java Collections Framework – JCF).

Viven en el paquete java.util.

🔹 Jerarquía principal

Collection → interfaz base de la mayoría de colecciones.

Métodos comunes: add(), remove(), size(), isEmpty(), iterator().

Subinterfaces más importantes:

List → colecciones ordenadas, admiten elementos duplicados.

Implementaciones: ArrayList, LinkedList, Vector.

Set → no permite duplicados.

Implementaciones: HashSet, TreeSet, LinkedHashSet.

Queue / Deque → estructura FIFO o doble cola.

Implementaciones: PriorityQueue, ArrayDeque.

Map (no hereda de Collection) → almacena pares clave-valor.

Implementaciones: HashMap, TreeMap, LinkedHashMap.


🔹 Ejemplo con List
import java.util.*;

public class EjemploList {
    public static void main(String[] args) {
        List<String> nombres = new ArrayList<>();
        nombres.add("Ana");
        nombres.add("Luis");
        nombres.add("Ana"); // admite duplicados

        for (String n : nombres) {
            System.out.println(n);
        }
    }
}

👉 Salida:

Ana
Luis
Ana


🔹 Ejemplo con Set
Set<Integer> numeros = new HashSet<>();
numeros.add(10);
numeros.add(20);
numeros.add(10); // ignorado, no se permiten duplicados

System.out.println(numeros); 


🔹 Ejemplo con Map
Map<String, Integer> edades = new HashMap<>();
edades.put("Juan", 20);
edades.put("Ana", 25);

System.out.println(edades.get("Ana")); // 25




🔹 Resumen 

List → ordenada, permite duplicados.

Set → no ordenada, sin duplicados.

Queue/Deque → manejo de colas.

Map → clave-valor (no extiende Collection).

Todas permiten recorrer elementos con for-each o Iterator.
