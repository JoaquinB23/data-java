Sobrecarga

🔹 ¿Qué es la sobrecarga?

La sobrecarga consiste en tener varios métodos con el mismo nombre en la misma clase, pero con distintos parámetros (tipo, cantidad u orden).

👉 Sirve para que un método pueda adaptarse a distintos tipos de datos o situaciones, sin necesidad de inventar un nombre nuevo para cada caso.

🔹 Reglas de la sobrecarga

Mismo nombre de método.

Parámetros diferentes:

Diferente tipo,

Diferente número, o

Diferente orden.

Tipo de retorno no es suficiente para sobrecargar.

Ocurre dentro de la misma clase (no requiere herencia).

🔹 Ejemplo básico
class Calculadora {

    // Suma de dos enteros
    int sumar(int a, int b) {
        return a + b;
    }

    // Suma de tres enteros
    int sumar(int a, int b, int c) {
        return a + b + c;
    }

    // Suma de dos números decimales
    double sumar(double a, double b) {
        return a + b;
    }
}

public class Main {
    public static void main(String[] args) {
        Calculadora calc = new Calculadora();

        System.out.println(calc.sumar(2, 3));       // 5
        System.out.println(calc.sumar(1, 2, 3));    // 6
        System.out.println(calc.sumar(2.5, 3.7));   // 6.2
    }
}


👉 Como ves, todos los métodos se llaman sumar, pero Java selecciona cuál usar según los parámetros que le pases.

🔹 Diferencia clave: Sobrecarga vs Sobreescritura
Característica	Sobrecarga (Overloading)	Sobreescritura (Overriding)
Clase	Misma clase	Subclase
Método	Mismo nombre, distintos parámetros	Mismo nombre, mismos parámetros
Tipo de retorno	Puede cambiar	Debe ser igual o compatible
Polimorfismo	Compilación	Ejecución